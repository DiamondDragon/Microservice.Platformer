{
  "baseAddress": "http://10.0.0.41:8200",
  "tokenVariableName": "VAULT_TOKEN",
  "configOverrides": [
    {
      "configKey": "connectionStrings:default",
      "vaultPath": "secret/dragon",
      "format": "server=(local);database=Administration;user={{ key1 }}; pwd={{ key2 }}"
    },
    {
      "configKey": "connectionStrings:default2",
      "vaultPath": "secret/dragon",
      "format": "server=(local);database=SomeDb;user={{ key1 }}; pwd={{ key2 }}"
    },
    {
      "configKey": "connectionStrings:default3",
      "vaultPath": "secret/dragon",
      "format": "server=(local);database=SomeDb;user={{ key1 }}; pwd={{ key2 }}"
    }
  ]
}
